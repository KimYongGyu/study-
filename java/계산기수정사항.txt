1. =이나 사칙연산을 수행시 중간 결과값 혹은 최종 결과값은 f1 텍스트 필드에 출력이 되야한다

2. 첫번째 연산자 입력 후 피연산자가 2자리 밖에 입력받지 못한다

3. 첫번째 연산 결과 후 추가적으로 입력시 f1필드(피연산자)가 초기화가 되지 않음

4. /연산시 소수점 2자리까지~~
DecimalFormat fmt = new DecimalFormat("0.###");
String decimal =  fmt.format(78.539816);

setText(fmt.format(78.539816));
